/**
 * popup.js
 * Popup window script.
 * Minimal — the CRM runs entirely as a content script.
 */

document.addEventListener('DOMContentLoaded', () => {
  // Future: query active tab status, show connection state, etc.
  console.log('[WA CRM] Popup loaded.');
});
